# Roadmap & Current State

> Единый источник правды: где мы сейчас и что дальше.
> См. также `BACKLOG.md` (детальный чеклист) и `IDEAS.md` (копилка идей).
> Последнее обновление: 2026-07-13.

## 🌍 Живой продукт
- **Сайт:** https://frag-platform.vercel.app (автодеплой с push в `main`)
- **Репо:** https://github.com/slogatskiy/frag_platform (публичный)
- **Каталог:** 59 424 аромата · ~5 300 с фото (наполняются on-demand) · у всех цена (оценочная)

## 🧱 Стек
Next.js 16 (App Router, TS, Turbopack) · Tailwind v4 · Prisma 6 · PostgreSQL (Supabase, eu-west-1) · Vercel (регион dub1) · шрифт Fraunces (serif) + тёмная премиум-тема. UI на English.

## ✅ Сделано
- Каталог: поиск + **сортировка** (Featured/Name/Newest/Oldest/Price)
- **Карточка аромата** `/fragrance/[slug]` — фото, ноты, цена, «кто из друзей владеет/хочет»
- **Discover по нотам** `/discover` — подбор по любимым нотам + сохранение в профиль
- **Полка** `/shelf` — коллекция, стоимость, сортировка, счётчик ×N, публичная страница `/u/[handle]`
- **Вишлист** `/wishlist` + сердечки в каталоге
- **Друзья** `/friends` — заявки/принятие, профиль друга со стоимостью полки
- **Логин** — email+пароль (Supabase Auth)
- **Фото** ~4 700 (топ-популярные) со страниц Parfumo
- **Цены — фаза 1**: оценка по тиру бренда × концентрации на все 59k (флаг `priceEstimated`, метка «est.»)
- **🫂 СОЦ-СЕТЬ (2026-07-12)**: **посты-впечатления** (рейтинг 1–10 + текст) — модели `Post`/`PostLike`. Лента `/feed` (единый таймлайн: посты друзей + активность коллекция/вишлист). **Шаримая страница поста `/p/[id]` с OG/Twitter-картой** (разворачивается ссылкой в соцсетях). Композер + отзывы на карточке аромата. Лайки (оптимистичные). «Feed» в хедере.
- **Новинки (кураторские)**: +заметные реальные релизы 2023–2025 через `prisma/seed-new.mjs` (без фото, est.-цена).
- **ЦЕНЫ v2 (2026-07-12)**: реалистичная оценка (`prisma/estimate-prices-v2.sql`) — 5 тиров брендов × концентрация × объём + детерминированный разброс. Убрала одинаковые $70. Диапазон $20–600, средняя $98.
- **ФОТО on-demand (2026-07-12)**: `Fragrance.sourceUrl` + `/api/photo` + `FragranceImage` — фото тянется при открытии аромата (Vercel тянет с чистых IP, Parfumo не банит). Организм. наполнение вместо массового скрейпа.
- **КОММЕНТАРИИ (2026-07-12)**: модель `Comment`, тред на `/p/[id]`, счётчик на карточках.
- **ФОТО-ПОСТЫ (2026-07-12)**: `Post.imageUrl` + Supabase Storage бакет `post-images` → Instagram-style карточки, фото поста в OG.
- **💸 COST-PER-WEAR (2026-07-13)**: грубая оценка `цена/(объём×2.5)` на карточке аромата и на полке.
- **🏆 РЕЙТИНГ ДРУЗЕЙ (2026-07-13)**: лидерборд по стоимости полки на `/friends` (места 🥇🥈🥉).
- **🎮 КВИЗ `/quiz` (2026-07-13)**: угадай бренд/ноту/что дороже; узнаваемые ароматы (`Fragrance.popularity` ≥300 из датасета); генерация в `src/lib/quiz.ts`.
- **⚔️ БАТТЛЫ `/battles` (2026-07-13)**: квиз-дуэли с другом (async, W/L/T, бейдж непройденных в хедере). Модель `Battle`.
- **📰 НОВОСТИ (2026-07-13)**: `NewsItem` + RSS-ингест (Now Smell This / Bois de Jasmin / ÇaFleureBon) `/api/news/ingest` (крон ежедневно) → `/news` + вплетены в `/feed`.
- **ЦЕНЫ фикс (2026-07-13)**: Louis Vuitton и др. престиж-дома добавлены в тиры (падали в дефолт $90).
- **ЦЕНЫ фаза 2 — реальные MSRP (2026-07-15)**: `prisma/real-prices.mjs` — кураторские настоящие ритейл-цены поверх оценок для ~218 флагманов (LV brand-wide $330, Creed, MFK, PdM, Tom Ford, топ-дизайнеры, Lattafa/Armaf). Устойчивый матчинг по нормализованному имени (срезает встроенный бренд/год/концентрацию/арабицу). Снимает флаг `priceEstimated`. Итого **243 аромата с реальной ценой**. UI: реальная цена — зелёный «✓ retail», оценочная — «~$X est.» (честно). LV теперь $330 (совпало с покупкой юзера).
- **АВАТАРКИ (2026-07-15)**: единый `<Avatar>` (детерминированный градиент из handle + инициал, поддержка загруженного фото) — заменил ad-hoc инициалы в post-card / feed / friends (лидерборд+списки) / профиль / хедер / комментарии.
- **NEWS РЕДИЗАЙН (2026-07-15)**: журнальные карточки (градиент-оверлей поверх «мыльных» RSS-превью, бейдж источника на фото, аккуратный фолбэк без фото), крупная featured-карточка + сетка на `/news`. Лента: активность с аватарками + compose-приглашение.
- **FRIENDS ПОЛИШ (2026-07-15)**: лидерборд с аватарками + полоски стоимости (relative bars), аватарки во всех списках.

## 🔜 Что дальше (приоритет сверху) — см. полный борд в `TASKS.md`
1. **🫂 Соц-доработки** — композер поста прямо в ленте + «аромат дня» (SOTD); подписки (follow) как в IG; уведомления.
2. **📰 Трекер ДРОПОВ брендов** (структурно, не только новости) + подписка на бренды.
3. **💰 Цены фаза 2** — реальные цены поверх оценок (упирается в скрейп-блок; нужен прокси/Playwright).
4. **🎮 Квиз/баттлы+** — больше типов вопросов, шэр результата постом, анти-чит в баттлах.
5. **📸 Больше фото + новинки 2025-26** — on-demand уже наполняет; массовый скрейп упирается в IP-бан Parfumo (нужен ротопрокси).
6. **🔐 Google OAuth** — код готов, нужен конфиг в дашбордах (пользователь отложил).
7. **✨ Полиш** — мобильное меню (в хедере много пунктов), баги, адаптив.

## ⚙️ Тех-заметки (важно для продолжения)
- **Секреты** в `/.env` (в .gitignore, НЕ в репо): DATABASE_URL (транзакц. пул :6543 ?pgbouncer=true), DIRECT_URL (session pool :5432), NEXT_PUBLIC_SUPABASE_URL/ANON_KEY. Те же ключи заведены в Vercel env.
- **Build gotcha (НЕ ломать):** `"build": "prisma generate && next build"` + postinstall — иначе Vercel падает на устаревшем Prisma-клиенте.
- **psql:** `/opt/homebrew/opt/libpq/bin` (быстрый COPY-импорт). Node 26 через brew.
- **Датасет** каталога: Parfumo tidytuesday 2024-12-10 (`parfumo_data_clean.csv`) — снимок на дек.2024, качать в scratchpad с raw.githubusercontent при надобности. **Нет ароматов 2025-26** (кроме ~215 доскрейпленных).
- **Скрипты** (`prisma/`): `gen-copy.mjs` + `estimate-prices.sql` (цены), `scrape-images.mjs` (фото по href-match, устойчив к rate-limit + валидация имени файла), `scrape-new.mjs` (новинки, объединение сортировок т.к. `current_page` на JS), `seed.mjs` (49 кураторских с реальными ценами).
- **Parfumo анти-бот:** периодически Cloudflare (403 «Just a moment») при наплыве — скрейперы медленные, с авто-стопом. Картинки хотлинкятся с media.parfumo.com (не скачаны к себе).

## Схема данных (Prisma)
User (+favoriteNotes), Brand, Fragrance (+retailPrice/retailVolume/priceEstimated/imageUrl/notes*), CollectionItem (+quantity/remainingPct), WishlistItem, Friendship (PENDING/ACCEPTED), **Post** (rating/body → user+fragrance), **PostLike** (unique user+post).
